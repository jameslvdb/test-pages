<!doctype html>
<html lang="en" class="h-full">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/prism-base16-monokai.dark.css' | url }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">
  </head>
  <body class="max-w-5xl mx-auto min-h-full flex flex-col">
    <header class="flex flex-wrap flex-shrink-0 justify-between pt-6">
      <div>
        <h1 class="text-xl font-bold">
          <a href="{{ '/' | url }}">{{ metadata.title }}</a>
        </h1>
      </div>

      {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
      <ul class="flex flex-wrap">
        {%- for entry in collections.all | eleventyNavigation %}
          <li class="{% if entry.url == page.url %} border-b-2 border-blue-600 {% endif %} mx-2">
            <a href="{{ entry.url | url }}" class="p-2">{{ entry.title }}</a>
          </li>
        {%- endfor %}
      </ul>
    </header>

    <br>

    <main class="max-w-3xl mx-auto flex-grow justify-items-center {% if templateClass %} {{ templateClass }}{% endif %}">
      {{ content | safe }}
    </main>

    {% include "footer.njk" %}

    <!-- Current page: {{ page.url | url }} -->
  </body>
</html>